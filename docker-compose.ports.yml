version: '3.8'

services:
  postgres:
    ports:
      - "5432:5432"

  backend:
    ports:
      - "8000:8000"
    environment:
      - FRONTEND_URL=http://localhost:3000

  frontend:
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:8000/api/v1
